return {
  "VidocqH/lsp-lens.nvim",

  opts = {
    sections = {
      definition = false,
      references = true,
      implements = true,
      -- Disable the following since it causes too many open files:
      git_authors = false,
    },
  },
}
