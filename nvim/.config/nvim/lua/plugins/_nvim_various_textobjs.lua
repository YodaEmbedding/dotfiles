return {
  "chrisgrieser/nvim-various-textobjs",

  opts = {
    keymaps = {
      useDefaults = true,
      disabledDefaults = { "r", "gc", "n" },
    },
  },
}

-- Potentially useful:
-- gG: entire buffer
-- ii: same indentation
-- aI: higher indentation
-- an: number, e.g. -1.34
