return {
  "echasnovski/mini.nvim",

  config = function()
    require("mini.comment").setup {
      -- options = {
      -- },
    }

    -- require("mini.pairs").setup {
    --   -- options = {
    --   -- },
    -- }
  end,
}
